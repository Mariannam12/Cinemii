// Backend API base URL.
// Note: we use 127.0.0.1 (not "localhost") on purpose — on this machine
// "localhost" can resolve to IPv6 ::1 and collide with other local services.
export const API_BASE = "http://127.0.0.1:8001";
